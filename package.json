{
  "name": "greenhouse",
  "version": "1.0.0",
  "description": "CLI-инструмент для синхронизации каталога WooCommerce через локальные JSON-снапшоты и таблицы.",
  "type": "commonjs",
  "scripts": {
    "pull":        "ts-node src/pull.ts",
    "normalize":   "ts-node src/normalize.ts",
    "sync-images": "ts-node src/sync-images.ts"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  }
}
